1. `.env` 파일을 생성하고 다음과 같이 Redis 연결 정보를 입력합니다:

   ```env
   REDIS_HOST=localhost
   REDIS_PORT=26379
   REDIS_PW=janek@123
   ```

2. 프로젝트 디렉토리에서 다음 명령어를 실행하여 필요한 패키지를 설치합니다:

   ```
   npm install
   ```

3. 개발 서버를 시작합니다:

   ```
   npm run dev
   ```
